{
  "name": "bastiondtl",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Motor C++20 para custodia segregada, liquidación firmada y control de liquidez.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/build.mjs && bun test --timeout 30000 ./tests/node ./sdk",
    "test:sdk": "bun test --timeout 30000 ./sdk",
    "typecheck": "bunx tsc --noEmit",
    "ci": "node scripts/build.mjs --warnings && bun run typecheck && bun test --timeout 30000 ./tests/node ./sdk && bun run format:check && bun run verify:repo",
    "format:check": "bunx prettier --check .",
    "verify:repo": "node scripts/verify-repository.mjs"
  },
  "devDependencies": {
    "@types/bun": "1.3.10",
    "@types/node": "26.2.0",
    "prettier": "3.9.5",
    "typescript": "5.9.3"
  },
  "engines": {
    "bun": ">=1.3.0",
    "node": ">=24"
  }
}
